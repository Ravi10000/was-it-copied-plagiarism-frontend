import SearchPage from "../search/search";

const postLoginPages = [
  {
    route: "/search",
    title: "Search",
    icon: "search",
    Component: SearchPage,
  },
];

export default postLoginPages;
